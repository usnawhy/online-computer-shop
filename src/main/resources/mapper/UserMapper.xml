<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.computershop.mapper.UserMapper">

    <resultMap id="userMap" type="User">
        <id property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="realName" column="real_name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="address" column="address"/>
        <result property="registerTime" column="register_time"/>
        <result property="status" column="status"/>
    </resultMap>

    <select id="findById" resultMap="userMap">
        SELECT * FROM user WHERE user_id = #{userId}
    </select>

    <select id="findByUsername" resultMap="userMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findAll" resultMap="userMap">
        SELECT * FROM user ORDER BY register_time DESC
    </select>

    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user(username, password, real_name, phone, email, address, register_time, status)
        VALUES(#{username}, #{password}, #{realName}, #{phone}, #{email}, #{address}, NOW(), 1)
    </insert>

    <update id="update" parameterType="User">
        UPDATE user
        <set>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="address != null">address = #{address},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE user_id = #{userId}
    </delete>

    <select id="login" resultMap="userMap">
        SELECT * FROM user WHERE username = #{username} AND password = #{password}
    </select>
</mapper>
